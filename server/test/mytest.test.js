// mytests.test.js

const assert = require('assert');
const sessionManager = require('../services/sessionManager.cjs');
// Ensure the correct relative path to reach the sessionManager module


describe('My Test Suite', () => {
    test('should pass this test', () => {
      expect(1 + 1).toBe(2);
    });
  });
  